odoo.define('cust_diskon','tour', function(require) {
    'use strict';
    const {_t} = require('web.core');
    const {Markup} = require('web.utils');
    var tour = require('web_tour.tour');
    
    const { markup } = owl;
    console.log("haii");
});